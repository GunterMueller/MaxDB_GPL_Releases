# parameter file for MUT on sqldbc

# number of Loops (-> test duration)
# default = 1
noOfLoops=10

# Kernel Mode
#   -f fast
#   -q quick
#   -s slow
# default = -f
&if $LC_STATE in [ DEV ]
kernelMode=-f
&else 
kernelMode=-f
&endif

