SELECT O."Order Date", "Count of Order ID" = Count(O."Order ID"), "Sum of Subtotals" = Sum(OS."Subtotal") FROM "Orders" O, "Order Subtotals" OS WHERE O."Order ID" = OS."Order ID" GROUP BY O."Order Date"