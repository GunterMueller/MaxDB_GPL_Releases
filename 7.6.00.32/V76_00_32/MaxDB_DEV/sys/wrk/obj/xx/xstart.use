start fast kernel   :: x_start <serverdb>
start quick kernel  :: x_start -quick <serverdb>
start slow kernel   :: x_start -slow <serverdb>
ignore autorestart  :: -i or -admin
start with low prio :: -l or -lowprio
verbose mode        :: -v
